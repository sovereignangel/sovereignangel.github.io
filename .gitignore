iProtoResume/
.next/
node_modules/
.env.local
.env*.local
out/
*.glb
.DS_Store

# Garmin sync secrets
scripts/.env
scripts/garmin_tokens/
scripts/firebase-service-account.json
.vercel

# Sensitive docs (keep local, don't publish)
LORI_PERSONAL_CONTEXT.md
MENTAL_OS.md
THESIS_ENGINE_PHILOSOPHY.md
THESIS_ENGINE_BRAND_STRATEGY.md
THESIS_ENGINE_PRD.md
REWARD_FUNCTION_ROADMAP.md
Becoming_Her_Daily_Practice.docx

# Log files
scripts/sync.log
scripts/garmin_sync.log

# Mahamudra recordings — served from Firebase Storage, never committed
app/mahamudra/recordings/*.m4a

# Its own git repository — a standalone project that happens to live here.
# Committing it from the parent would only record a broken gitlink.
connection-insights/

# Local incremental-build cache
*.tsbuildinfo

# Finance — statements, ledgers and tax documents stay local, never committed
app/finance/data/*
!app/finance/data/README.md

# travel planning renders (PDF/PNG/HTML are outputs, not sources)
scripts/travel/out/
__pycache__/

# local book library — copyrighted PDFs and their extracted text.
# The corpus is local-first: never committed, never deployed.
# Rebuild with: node scripts/books/extract.mjs
app/books/*.pdf
data/books/
